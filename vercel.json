{
  "installCommand": "cd client && npm install --no-audit --no-fund",
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/client/dist/index.html"
    }
  ]
}
